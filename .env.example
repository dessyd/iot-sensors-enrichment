# Example environment variables for the project (no secrets)
# Copy this to .env and fill the secrets for local development.

ADMIN_PASSWORD=changeme
# DATABASE_URL=sqlite:///./iot_enrichment.db
SQLITE_FILE=./iot_enrichment.db

# Replace with a secure value in production
JWT_SECRET=replace-with-secure-secret
JWT_ALGORITHM=HS256
JWT_EXPIRATION=60
